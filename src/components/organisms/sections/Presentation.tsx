import styled from "styled-components";

const StyledPresentation = styled.section`

`;

const Presentation: React.FunctionComponent<void> = () => {

    return (
        <StyledPresentation>

        </StyledPresentation>
    );

};

export default Presentation;